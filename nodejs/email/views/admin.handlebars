!!! 5
html(lang="en")
  head
    title= title
    script(type='text/javascript')
    script(type='text/javascript')
      var socket = io.connect('http://'192.168.100.25':3000');
      socket.on('connect', function () {
        $("#btn-set").click(function(){
          var $stime = $('#starttime').val(),$etime = $('#endtime').val(),$pid = $('#pid').val();
            if($etime!==""&&$pid!==""){
              socket.emit("_setprice",{__st:$stime,__et:$etime,__id:$pid})
            }
            $('#starttime').val("");
            $('#endtime').val("");
            $('#pid').val("");
        });

      socket.on('send_pid',function(result){
        var pid_+(new Date()).getTime() = new Timer(result.stime,"timeshow",function(){alert("time out")
        $('span#pname').html(result.pname);
        $('span#prise').html(result.prise);
      })


      });


  body

    #container
      h1=title
      - if (youAreUsingJade)
        p
          设置一个促销/活动
      - else
        p oooooooooooooooooooooooooooooooooo
      div
        form#manager-action
          开始时间：
          input(type='text',value='',id='starttime',placeholder='为空则计时从当前时间开始')
          br
          到期时间：
          input(type='text',value='',id='endtime',placeholder='必填')
          br
          &nbsp;&nbsp;&nbsp;商品ID：
          input(type='text',value='',id='pid',placeholder='必填')
          br
          input(type='button',value='提交',id='btn-set')

          p#res
            span#timeshow
            br
            商品：span#pname
            br
            价格：span#prise


    p Powered By &nbsp;
      a(href='http://socket.io') Socket.io

    footer Copyright©2012 zhangJianqiang